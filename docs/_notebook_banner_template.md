<!-- Copy the block below into the first markdown cell of a tutorial notebook if helpful. -->

> **Source:** This notebook tracks **`main`**. Published HTML for **`stable`** (releases) and **`latest`** (development) can differ; use the docs **version menu** and install a matching package. See [Versioned documentation](versioning.md).
