# Utils

Shared helpers for simulators, filtering, and model validation. The public entry point [`flatten_draws`](../../public/simulators/overview.md) is re-exported from the top-level `dynestyx` package.

::: dynestyx.utils
    options:
      filters: []
