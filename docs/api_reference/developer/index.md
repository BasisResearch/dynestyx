# Private (Developer) API

We break the API documentation into a [public/user API](../public/index.md), for users of the PPL, and a private/developer API (this one). The public/user API is useful for general algorithmic reference for users, whilst the private/developer API is more useful for those wishing to implement new features in `dynestyx`.

